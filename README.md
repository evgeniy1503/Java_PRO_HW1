–еализовать приложение, способное Ђпрогон€ть тестыї.

Ј        —оздаете набор аннотаций: @Test, @BeforeSuite, @AfterSuite, которые могут добавл€тьс€ на методы, @Test на обычные методы, @BeforeSuite и @AfterSuite только на статические.

Ј        ƒл€ аннотации @Test добавл€ете параметр priority (int в пределах от 1 до 10 включительно). ≈сли параметр не задан €вно, то он равен 5.

Ј        —оздаете класс с набором методов, и размечаете их созданными аннотаци€ми.

Ј        —оздаете класс TestRunner со статическим методом runTests(Class c).

Ј        ¬ыполнение runTests(Class c) приводит к следующему:

o   ”казанный класс загружаетс€ в пам€ть

o   ѕроисходит проверка что методов с аннотаци€ми @BeforeSuite не больше одного

o   ѕроисходит проверка что методов с аннотаци€ми @AfterSuite не больше одного

o   ѕримечание: ћожете выполнить дополнительные проверки, которые посчитаете нужными

o   ¬ыполн€етс€ метод с аннотацией @BeforeSuite, если такой есть

o   ¬ыполн€ютс€ методы с аннотаци€ми @Test в соответствии с их приоритетом. ¬начале выполн€ютс€ те методы, у которых приоритет выше.

o   ¬ыполн€етс€ метод с аннотацией @AfterSuite, если такой есть

Ј        * ћожете добавить аннотации @BeforeTest и @AfterTest, методы с такими аннотаци€ми должны выполн€тьс€ перед каждым и после каждого теста соответственно.

Ј        * ƒобавьте аннотацию @CsvSource параметром которой €вл€етс€ строка. ѕри запуске метода-теста эта строка должна распарситьс€, каждый элемент строки приведен к типу соответствующего аргумента метода и метод выполнен с данными из указанной строки.

Ќапример: @CsvSource("10, Java, 20, true")

public void testMethod(int a, String b, int c, boolean d) { .. }